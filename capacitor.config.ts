import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'morellis-food-ordering',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
